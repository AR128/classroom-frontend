import { CreateButton } from "@/components/refine-ui/buttons/create";

const ClassesList = () => {
  return (
    <div>
      <div>List</div>
      <CreateButton />
    </div>
  );
};

export default ClassesList;
